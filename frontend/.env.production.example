# Used by `npm run build` to bake values into the static export.
# Frontend is served by FastAPI on the same origin as the API, so the
# API base must be empty (relative URL = same origin).

NEXT_PUBLIC_API_BASE=
NEXT_PUBLIC_BASE_PATH=
